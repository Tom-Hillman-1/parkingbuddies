-- add multiple listings to spotdetailsview page, if more then one is pending/approved. add all.
-- change dashboard to 3 main categories "owner" "driver" "transactions" and put the relvent current cards under each one.
-- add email form from formsubmit, sending emails after sign up, and maybe validating the account through email.
-- move "saved" tab a little up as it is overlayed on the tab below. inside of settings, after saving changes
-- cookies (??)
-- consider adding service fee so users get "full amount"
-- consider adding log in with google, and also checkout as guest.
-- consider moving search bar and filter to the top to give more space for listings  (i.e JustPark)
-- add over hover pin on map to show space details. change the colour when hovering that listing so it pops out on the map.
-- be able to click a listing to go to details page, insted of being able to only press "view details" ->consider removing button.
-- change completly about us page, make it a lot more clear, and usefull info for users, make it quite big with a lot og details ; how you will be charged, points explained, about the project, and a lot of other details.
-- change text formatting to be consistent in bubbles "Rent" instead of "rent" per Hour, not hour
-- turn pin lng lat -> nearest location, so that it is searchable for users.
-- if user didn't enter details correctly, take him to that part that the error says of the page after pressing "create listing"
-- make select listing type its own tab (1st card) inside of create lisitng page
-- make it so that when pressing on the user name/pts in nav bar, it takes users to rewards section inside of dashboard.
-- wrap "Points are disabled for free listings" correctly inside of "set point price" in "create listing"
-- make more phone accesability. especially nav bar. also toggle view is covered by map when in phone view